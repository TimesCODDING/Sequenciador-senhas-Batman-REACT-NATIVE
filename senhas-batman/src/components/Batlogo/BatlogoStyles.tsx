import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  titulo: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 16,
    color: '#B29146'
  },

  imagem: {
    resizeMode: 'contain',
    height: 180,
  }
});